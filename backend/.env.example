# Cloudflare Tunnel token, get it from:
# https://one.dash.cloudflare.com -> Networks -> Tunnels -> Create a tunnel -> select Docker
# Copy the long string after `--token` (it starts with "eyJ...")
TUNNEL_TOKEN=
